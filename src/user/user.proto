syntax = "proto3";

package user;

service UserService {
    rpc FindOne (UserId) returns (User);
}

message UserId {
    string id = 1;
}

message User {
    string id = 1;
    string name = 2;
    string email = 3;
    string password = 4;
    bool verify = 6;
    string avatar = 7;
}